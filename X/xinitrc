# Set terminal options
[[ -f ~/.Xresources ]] && xrdb -merge ~/.Xresources &

xset +fp /usr/share/fonts/local
xset fp rehash

# ???
pulseaudio --start &

# swap caps and ctrl
setxkbmap -option "ctrl:swapcaps"

exec ~/.config/bspwm/start_bspwm
