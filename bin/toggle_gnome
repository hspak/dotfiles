#!/bin/bash

if [[ $EUID != 0 ]]; then
  echo "requires root privileges (for systemd)"
  exit 1
fi

if [[ "$1" == "on" ]]; then
  systemctl disable autologin@tty1
  systemctl enable gdm
else
  systemctl enable autologin@tty1
  systemctl disable gdm
fi
