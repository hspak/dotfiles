#!/bin/bash

artist="Mpd"
title="Nothing's playing..."

mpc | grep playing &> /dev/null
if (( $?==0 )); then
    artist="$(mpc -f %artist% current)" 
    title="$(mpc -f "[[%album% - ]%title%]|[%file%]" current)" 
fi

notify-send "$artist" "$title"
