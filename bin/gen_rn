#!/bin/bash

if (( $# < 1 )); then
  echo "usage: $0 <output file> <number count>"
  exit 1
fi

if [[ -f "$1" ]]; then
  echo "file already exists, replacing with new"
  rm "$1"
fi

bytes=$(($2*16))
od -vN$bytes -An -i /dev/urandom >> "$1"
